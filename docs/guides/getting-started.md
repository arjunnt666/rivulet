# getting started

```bash
cargo build -p rivulet-cli
cargo run -p rivulet-cli -- version
```

js side:

```bash
pnpm install
pnpm --filter @rivulet/js build
```

then open an example and break things.
